/// <reference types="react" />
interface OptionProps {
    value: string;
    label?: string;
}
interface Props {
    name: string;
    options: OptionProps[];
    multiple?: boolean;
}
declare type ChoiceProps = JSX.IntrinsicElements['input'] & Props;
export default function Choice({ name, options, multiple, ...rest }: ChoiceProps): JSX.Element;
export {};
